# ===== Required integrations =====
ZABBIX_URL=https://zabbix.example.com
ZABBIX_TOKEN=replace_me
UPTIMEROBOT_API_KEY=replace_me
TEAMS_WEBHOOK_URL=https://your-tenant.webhook.office.com/webhookb2/...

# ===== Time/correlation controls =====
LOOKBACK_MINUTES=30
TIME_WINDOW_MINUTES=10

# ===== Performance controls =====
HTTP_TIMEOUT=20
BATCH_SIZE=20
MAX_CONCURRENCY=8
ENRICH_LOOKBACK_MINUTES=20
ENRICH_TOP_N_ITEMS=5
ENRICH_ITEM_KEY_HINTS=system.cpu,vm.memory,vfs.fs,net.if,proc.num,log,kubelet,docker,mysql

# ===== SQLite cache =====
CACHE_DB=rca_cache.db
TTL_ZABBIX_SEC=90
TTL_UPTIME_SEC=180
TTL_LLM_SEC=600

# ===== LLM (optional, required for rca_multi_agent.py intelligence) =====
# If empty, stdlib script uses fallback RCA text without LLM call.
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_URL=https://api.openai.com/v1/chat/completions

# ===== Ollama (OpenAI-compatible endpoint) =====
# Use these if you want local inference via Ollama.
# Example (Linux/macOS):
#   LLM_API_KEY=ollama
#   LLM_MODEL=qwen2.5:7b-instruct
#   LLM_URL=http://127.0.0.1:11434/v1/chat/completions
#
# On Windows Docker/WSL setups, 127.0.0.1 may differ.
# You can use host.docker.internal or server LAN IP if needed.

# ===== ServiceDesk Plus v14720+ (optional) =====
# If these 3 vars are set, script runs:
# update solution -> add 1 task -> close task -> add worklog -> close ticket
SDP_URL=https://helpdesk.example.com
SDP_TECHNICIAN_KEY=replace_me
SDP_REQUEST_ID=123456  # fallback only; preferred: pass via input JSON/--request-id
SDP_TASK_TITLE=RCA investigation
SDP_TASK_OWNER=
SDP_RESOLUTION_PREFIX=[AUTO RCA]
SDP_CLOSE_STATUS=Closed
